//Minimum characters to be added at front to make string palindrome:
#include<iostream>
#include<string>
using namespace std;
int makingPalindrome(string s){
    for(int i=0;i<s.length();i++){

    }
}
int main(){
    string s="ABC";
    makingPalindrome(s);
}